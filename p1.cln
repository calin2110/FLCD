fun main(): void {
    def a: int;
    read(a);
    def b: int;
    read(b);
    def c: int;
    read(c);
    
    final max: int;
    unless (a > b) {
        unless (b > c) {
            max = c;
        } else {
            max = b;
        }
    } else {
        unless (a > c) {
            max = c;
        } else {
            max = a;
        }
    }
    
    write(max);
}