fun main(): void {
    def length: int;
    write("len_is");
    read(length);
    
    def array: int[length];
    def i: int;
    
    forenum (i in length) {
        read(int[i]);
    }
 
    def sum: int = 0;
    foreach(i in array){
        sum += i;
    }
    
    write(sum);
}